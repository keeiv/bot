[build-system]
requires = ["setuptools>=80", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "discord-bot"
version = "1.0.0"
description = "A feature-rich Discord bot with osu! integration and GitHub monitoring"
authors = [{name = "Finn", email = "finn@example.com"}]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    "aiohttp>=3.14.3,<4",
    "cryptography>=50.0.1,<51",
    "deep-translator>=1.11.4,<2",
    "discord.py>=2.7.1,<3",
    "genshin>=1.7.30,<2",
    "ossapi>=5.3.6,<6",
    "psutil>=7.2.2,<8",
    "python-dotenv>=1.2.3,<2",
]

[project.optional-dependencies]
music = ["wavelink>=3.4.1,<4"]
dev = [
    "bandit>=1.9.4,<2",
    "black>=26.5.1,<27",
    "flake8>=7.3.0,<8",
    "flake8-bugbear>=25.10.21,<26",
    "flake8-comprehensions>=3.17.0,<4",
    "flake8-docstrings>=1.7.0,<2",
    "isort>=5.13.2,<6",
    "mypy>=2.3.1,<3",
    "pre-commit>=4.6.2,<5",
    "pytest>=9.1.1,<10",
    "pytest-asyncio>=1.4.0,<2",
    "pytest-cov>=7.1.0,<8",
    "safety>=3.8.1,<4",
]

[project.scripts]
bot = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.black]
line-length = 88
target-version = ['py310']
include = '\.pyi?$'
# Support multilingual code formatting
skip-string-normalization = false
force-exclude = '''
/(
    \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''

[tool.isort]
profile = "google"
multi_line_output = 3
line_length = 88
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
# Multilingual import organization
known_first_party = ["src"]
known_third_party = ["discord", "aiohttp", "ossapi"]

[tool.flake8]
max-line-length = 88
extend-ignore = [
    "E203",  # whitespace before ':'
    "W503",  # line break before binary operator
    "D100",  # Missing docstring in public module
    "D101",  # Missing docstring in public class
    "D102",  # Missing docstring in public method
    "D103",  # Missing docstring in public function
    "D104",  # Missing docstring in public package
    "D105",  # Missing docstring in magic method
    "D107",  # Missing docstring in __init__
    "D202",  # No blank lines allowed after function docstring
    "D212",  # Multi-line docstring summary should start at the first line
    "D415",  # First line should end with a period, question mark, or exclamation point
]
exclude = [".git", "__pycache__", ".venv", "build", "dist"]
# Support multilingual naming conventions
docstring-convention = "google"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
# Support multilingual type hints and documentation
check_untyped_defs = true
disallow_any_generics = true
disallow_subclassing_any = true
disallow_untyped_calls = true
disallow_incomplete_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
